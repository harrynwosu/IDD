{
    "routes": [
        {
            "src": "/[^.]+",
            "dest": "/index.html",
            "status": 200
        },
        {
            "src": "/api/(.*)",
            "dest": "/server/server.js"
        }
    ]
}
